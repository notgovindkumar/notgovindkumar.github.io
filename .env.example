# Add these in Vercel → Project Settings → Environment Variables.
# Do not commit a real .env file or expose these values in client-side JavaScript.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=
